# 生产环境：需配置 MySQL 等
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/credit?useUnicode=true&characterEncoding=utf8}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  sql:
    init:
      mode: never

logging:
  level:
    com.winder: INFO
